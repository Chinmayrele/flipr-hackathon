class Dish {
  String id;
  String title;
  int price;
  bool isAvaliable;

  Dish(
      {required this.id,
      required this.title,
      required this.price,
      required this.isAvaliable});
}
